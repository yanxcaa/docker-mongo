version: "3.9"

services:
  my-node-app:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "3000:3000"
    restart: always
#    volumes:
#      - mongo-data: /data/db
#      - mysql: /var/lib/mysql
#      - postgres: /var/lib/postgresql/data
#    depends_on:
#      - mongo-example
#  mongo-example:
#    image: mongo:18
#    ports:
#      - "27017:27017"
#    environment:
#      MONGO_INITDB_ROOT_USERNAME: yanca
#      MONGO_INITDB_ROOT_PASSWORD: MongoPassword

# You define global volumes at the very bottom

#volumes:
#  mongo-data: